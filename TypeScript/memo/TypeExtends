타입 확장은 기존 타입을 사용해서 새로운 타입을 정의하는 것을 말한다.
기본적으로 타입스크립트에서는 interface와 type 키워드를 사용해서 타입을 정의하고 extends, 교차 타입, 유니온 타입을 사용하여 타입을 확장한다.
타입 확장은 불필요한 코드 중복을 줄일 수 있다.

유니온 타입
A | B로 쓴다. 타입의 합집합이다.

교차타입
A & B로 쓴다. 타입의 교집합이다.

extends 키워드를 사용하면 교차 타입과 유사한 역할을 수행한다.
extends로도 교차 타입을 만들수 있지만 100% 똑같지는 않다.
