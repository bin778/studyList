연산자
	1. 피연산자의 타입
	2. 연산의 동작
	3. 연산 결과 값의 타입
	산술연산자
		1. 피연산자 타입이 서로 바뀌어도 결과타입은 동일
		2. 덧셈에서 String이 사용되면 결과는 String
		3. char에 해당하는 아스키코드 int값으로 생각
		4. 피연산자 어느 한쪽이 double이라면 결과는 double 타입
		+, -, *, /, %
	관계연산자
		char, int, double 모두 혼용하여 사용 가능
		String은 사용 불가능
		a > b : a가 b보다 크니? 맞으면 true, 아니면 false
		a < b : a가 b보다 작니? 맞으면 true, 아니면 false
		a >= b : a가 b보다 크거나 같니? 맞으면 true, 아니면 false
		a <= b : a가 b보다 작거나 같니? 맞으면 true, 아니면 false
		a == b : a가 b랑 같니? 맞으면 true, 아니면 false
			문자열은 서로 같은지 확인하기 위해서 equals()메소드를
			사용해야 한다 ★★★★
		a != b : a가 b랑 다르니? 맞으면 true, 아니면 false
	★★관계연산의 결과는 boolean 타입이다
  	논리연산자
		a && b	: and연산
			a 그리고 b 둘 다 true라면 결과는 true
			아니면 false
		a || b	: or 연산
			a 혹은 b 둘 중 하나라도 true이면 결과는 true
			아니면 false
		!a	: not 연산
			a가 true라면 결과는 false,
			a가 false라면 결과는 true
		★논리연산의 결과는 boolean 타입의 값
		논리연산의 피연산자는 boolean타입이 와야 한다
		!(10 != 5) && 7 % 2 == 1 --> false
	삼항연산자
		조건식 ? 값1 : 값2
		만약 조건식이 true라면 결과는 값1
		조건식이 false라면 결과는 값2
	조건식
		참 또는 거짓을 판별할 수 있는 식
		(boolean 타입이 와야한다)

연산자의 우선순위
	최우선 연산자	()
	단항 연산자	!, (int), ...
	산술 연산자	+-*/%
	쉬프트 연산자	
	관계 연산자	> < >= <= != ==
           논리 연산자		&& ||
	삼항 연산자	?:
	대입 연산자	=
