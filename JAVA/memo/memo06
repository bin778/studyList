for문
	for(초기식; 조건식; 증감식){
		반복할문장;
	}

	for문을 만나면 
	1. 초기식을 실행한다
	2. 조건식을 확인한다
		2-1. 조건식이 true라면 for문 안쪽으로 들어와서
		for문 안쪽 문장을 수행하고, for문 닫는중 괄호를 만나면
		(for문이 끝나면) 증감식을 수행한다. 그리고 2 반복

		2-2. 조건식이 false라면 for문 안쪽으로 들어가지 않는다
		(반복종료)
continue
	자신을 감싸는 가장 가까운 반복문의 즉시 다음 반복을 실행한다
이중 for문
	for문 안에 있는  for문

복합대입연산자
	변수 += 값 --> 변수 = 변수 + 값
	변수 -= 값 --> 변수 = 변수 - 값
	변수 *= 값 --> 변수 = 변수 * 값
	변수 /= 값 --> 변수 = 변수 / 값
	변수 %= 값 --> 변수 = 변수 % 값

증감연산자
	후위형
	num++ --> num += 1 --> num = num + 1
	num-- --> num -= 1 --> num = num - 1
	
	전위형
	++num
	--num
  
  while문
	while(조건식){
		반복할문장;
		반복할문장;
		...
	}

	while문을 만나면
	1. 조건식을 확인한다
	2-1. 조건식이 true라면 while문 안쪽으로 들어와서 문장들을 수행하고
		while문 닫는 중괄호를 만나면 1 반복
	2-2. 조건식이 false라면 while문 안쪽으로 들어가지 않는다(반복종료)

while vs for
	반복의 횟수를 알고 있다면 for문
	반복의 조건을 알고 있다면 while문

continue
	자신을 감싸는 가장 가까운 반복문의 즉시 다음 반복을 실행한다
break
	자신을 감싸는 가장 가까운 반복문을 즉시 종료(탈출)한다

do~while문
	do{
		반복할문장;
		...
	}while(조건식);

	while문은 조건식을 먼저 확인하고, 반복여부를 결정하는 반면에
	do~while문을 일단 한번 먼저 실행하고, 그 뒤에 조건식을 확인하여
	반복 여부를 결정한다
