상속(inheritance)
	부모클래스로부터 자식클래스가 코드를 물려받는 문법

	1. 상속을 받은 자식 클래스 타입 인스턴스에서는 부모클래스에
	존재하는 멤버들을 사용할 수 있다
	2. 자식 클래스에서 새로운 멤버(멤버 변수, 메소드)들이 필요하다면
	추가해준다(자식클래스 타입 인스턴스에서만 사용가능)
	3. 부모한테 물려받은 메소드가 자식에서 다르게 동작해야한다면
	같은 이름으로 재정의(Overriding)하면 된다
	(부모타입인스턴스에서는 부모에서 정의된대로,
	자식타입 인스턴스에서는 자식에서 재정의대로 동작한다)

생성자
	상속받은 자식 생성자 내부에서 가장 먼저 부모 생성자를
	호출해야만 한다

super()
	부모클래스의 생성자를 의미한다
	자식 클래스 생성자를 정의할 때 가장 윗 줄에 부모생성자가 써있지 않다면
	알아서 부모기본생성자(super()) 한줄을 추가해준다
	그러나 부모의 기본생성자가 없다면 super()를 사용할 수 없고,
	그럴 때는 직접 명시적으로 정의된 부모의 생성자를 직접 써야한다.

Object클래스
	자바의 최상위 클래스,
	모든 클래스의 가장 위쪽 부모 클래스는 Object 클래스이다
	
-------------------------------------------------------------------------
오버로딩 vs 오버라이딩
	오버로딩 (중복정의)
		메소드를 똑같은 이름으로, 매개변수 갯수나 타입을 다르게
		중복하여 정의하는 행위(상속과 관계X)
	오버라이딩 (재정의)
		부모에서 상속받은 메소드를 같은 이름으로 다시 정의하는 행위
		오버로딩과 오버라이딩 모두 다형성을 구현하는 요소 중 하나이다

인스턴스(객체)의 타입
	인스턴스 instanceof 클래스이름
		인스턴스가 해당클래스 타입이라면 true, 아니면 false

	상속받은 자식은 자식 타입이면서, 부모 타입이다

업캐스팅 다운캐스팅
	업캐스팅
		자식 타입 인스턴스를 부모로 바꾸는 문법
		업캐스팅되면 자식에서 새롭게 만든 멤버는 사용 불가
		자식에서 오버라이딩 한 메소드는 오버라이딩 한 대로 동작
	다운캐스팅
		업캐스팅된 인스턴스를 다시 자식으로 되될리는 문법
		다운캐스팅되면 사용하지 못했던 자식에서 새롭게 만든
		멤버를 사용할 수 있다
