조건문
	조건에 따라서 다른 코드를 실행시키고 싶을 때 사용한다

if문
	if(조건식) {
		if문안쪽문장;
		if문안쪽문장;
		..
	}
	if문바깥쪽문장;

	if문을 만나면 조건식을 확인한다
	조건식이 참이라면 if문 안쪽 문장을 차례로 수행한다
	조건식이 거짓이라면 if문 안쪽 문장을 수행하지 않는다
  
  if문
	if(조건식) {
		조건식이참일때실행되는영역;
		....
	}else {
		조건식이거짓일때실행되는영역;
		....
	}

else-if문
	if(조건식1){
		조건식1이 참일때 실행되는 영역
	}else-if(조건식2){
		조건식1은 거짓이고 조건식2가 참일때 실행되는 영역
	}else-if(조건식3){
		조건식1, 조건식2는 거짓이고 조건식3이 참일때 실행되는 영역
	} ...{
		...
	}else{
		위에있는 조건식이 모두 거짓일때 실행되는 영역
	}

삼항연산자 vs if문
	모든 삼항연산자는 if문으로 바꿀 수 있다
	모든 if문을 삼항연산자로 바꿀수는 없다

switch문
	switch(표현식){
	case 값1:
		표현식 == 값1일때 실행할 영역;
	case 값2
		표현식 == 값2일때 실행할 영역;
	...:
		....
	default:
		표현식이 위에있는 값과 모두 다를때 실행하는 영역;
	}
