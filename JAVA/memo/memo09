클래스(class)
	여러개의 값을 저장하는, 다른타입의 값도 저장할 수 있는 배열
	메소드도 저장할 수 있다...
	
	클래스는 사용자가 만드는 새로운 유형의 자료형(type)이다

	암목적 약속 : 대문자로 시작한다

클래스 정의
		class 클래스이름{
		변수;
		메소드() {

		}
		....
	}

생성자
	클래스이름뒤에소괄호
	해당 클래스 타입의 객체(인스턴스)를 생성해준다

클래스필드
	멤버
		멤버 변수
			인스턴스변수
			클래스변수
		멤버 함수==메소드
    
    기본생성자
	명시적으로 정의된 생성자가 없다면 컴파일러가 알아서
	기본 생성자를 추가해준다

초기화 생성자
	인스턴스를 만듦과 동시에 인스턴스 변수에다 초기값을 설정할 수 있는
	생성자

this
	자기 자신 인스턴스(객체)를 의미한다.

static 제어자
	지역변수를 마치 전역변수처럼 활용할 수 있도록 만드는 제어자

접근제어자(access modifier)
			같은패키지 같은패키지
			같은클래스 다른클래스
	public		O	   O
	protected		O	   O
	default		O	   O
	private		O	   X

----------------------------------------------------------------------------
객체(인스턴스)가 요소로 들어있는 배열
