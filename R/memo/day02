read.table('경로'): 해당 파일을 테이블(표) 형태로 읽어온다.
	옵션 목록
	header: 파일속에 컬럼이름(헤더) 포함여부를 설정한다.
	sep: 구분자를 설정한다.
	csv파일은 ','가 구분자다.

is.na() 함수: 걸측값 여부를 파악한다. 결과는 T, F

View() 함수: 결과를 알아보기 쉽게 표 형태로 볼 수 있다.

mean: 데이터의 평균을 구한다.
sum: 데이터의 전체 합을 구한다.
데이터$요소 -> ex) scores$math

데이터 전처리
	분석하려는 데이터를 추출, 가공, 수정하는 과정
	데이터 분석의 80% 정도를 차지하는 중요하고 고된 과정

- grep(조건, 데이터)
	데이터에서 조건에 해당하는 데이터만 검색하는 함수

- strrep(문자, 반복 횟수)
	해당 문자를 횟수만큼 반복하여 하나의 문자로 돌려준다.

데이터 확인하기(파악하기)
- str(데이터)
	데이터 요약 결과 보기

- View(데이터)
	데이터를 표 형태로 보기

- head(데이터), head(데이터, n)
	데이터 중 상위 n개를 뽑아서 볼 수 있다.


dplyr 패키지
	rename() : 이름 변경
	filter() : 행 골라내기
	select() : 열 골라내기

파이프 연산자(%>%)
	함수의 결과를 파이프로 연결해서 다음 함수로 보내준다.
	데이터가 흘러가는 모습을 상상하면 된다.

파생변수
	기존의 변수를 조합하여 새로운 변수를 만들어 내는 것
	주관적일 수 있기 때문에 논리적 타당성 혹은 통계성 유의성에 주의해야 한다.
